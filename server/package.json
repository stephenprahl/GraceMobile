{
  "name": "server",
  "version": "1.0.0",
  "description": "GraceMobile Backend Server",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "@prisma/client": "^6.13.0",
    "dotenv": "^17.2.1",
    "elysia": "^1.0.33",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "prisma": "^6.13.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
